/*
C Program to demonstrate functions: Find sum of digits (recursive & non-recursive)
*/
#include <stdio.h>

// Define recursive function to calculate sum of digits
// Define iterative function to calculate sum of digits
int main() {
    // Take user input
    // Call both functions and display results
    return 0;
}
